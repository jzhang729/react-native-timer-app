import React from "react";
import { Text } from "react-native";

interface Props {
  isOpen: boolean;
}

const ToggleableTimerForm: React.FC<Props> = () => {
  return <Text>ToggleableTimerForm</Text>;
};

export default ToggleableTimerForm;
